# フィーチャーフラグ — コードを出すことと機能を見せることを分ける

## 今日のゴール

- コードを本番に出すことと、機能を利用者に見せることは分けられると知る
- フラグは `if` 文だが、値がコードの外から来る点が違うと分かる
- 消す条件を決めないと、残ったフラグが事故になると知る

## 出したものはそのまま見えてしまう

本番に出したものは、そのまま利用者に見えます。だから「まだ見せられない機能」は本番に入れられません。作りかけのものは自分のブランチ（本番のコードから分けた、自分用の作業コピー）で持っておいて、完成してから出す。ふつうはそう考えます。

同じ理由で、機能をオフにしたいときもコードを直すことになります。挙動をひとつ変えるために、ビルドし直して、確認して、また本番に出す。

**この2つを分けられるようにする**のが**フィーチャーフラグ**です。フィーチャートグルとも呼ばれます。コードは本番に置いたまま、機能だけ見せない状態を作れます。オンとオフの切り替えに、コードの変更もビルドも要りません。

## 分岐にひとつ挟むだけ

やることは、機能をひとつの分岐で包むことです。

```jsx
import { isEnabled } from "./flags";

export default function OrderPage() {
  return (
    <div>
      <OrderSummary />
      {isEnabled("new-payment-flow") && <NewPaymentFlow />}
    </div>
  );
}
```

これで新しい決済フローのコードは本番に入っていますが、`isEnabled` が false を返すあいだ画面には出てきません。完成したらオンにするだけで、コードは触りません。

## 値がコードの外から来る

書き方としては `if` 文です。違うのは、**その値がコードの中に書かれていない**ことです。値の置き場所は環境変数だったり、データベースや専用のサービスだったりしますが、どれにしてもコードの外にあります。

値をコードに書いてしまうと（`const ENABLED = false` のように）、それはフラグではなく普通の分岐です。切り替えるたびにコードの変更が必要になります。

値が外にあるから、コードを変えずにオンとオフを行き来できます。さらに置き場所によっては、**人によって違う値を返せます**。社内の人だけオン、10%の利用者だけオン、という出し分けです。

- **環境変数**: いちばん手軽。ただし値を変えるには再起動が要り、全員が同じ値になる
- **データベースや専用のサービス**: 値を変えるとすぐ反映され、利用者ごとに変えられる

## 分けられると何ができるか

**未完成を本番に置ける。** 作りかけのコードを、オフのまま統合先（チーム全員の変更が集まるブランチ）に入れておけます。これができないと、完成までブランチで抱えることになります。抱えているあいだも他の人は統合先を変え続けるので、ブランチは日が経つほど周りから離れます。離れたぶんのズレは最後のマージでまとめて解消することになり、3日分と3か月分では衝突の数も1件ごとの難しさも変わります。

<svg viewBox="0 0 420 300" role="img" aria-labelledby="d178-t d178-d" style="width:100%;max-width:520px;height:auto;display:block;margin:1.5rem auto;">
  <title id="d178-t">未完成のコードの置き方2通り</title>
  <desc id="d178-d">ブランチに抱える場合、作業ブランチは統合先から日が経つほど離れ、最後に大きなマージが必要になる。フラグでオフにして入れる場合、コードは毎日統合先に入り、機能だけがオフのまま待つ。</desc>
  <g fill="currentColor" font-size="11" opacity="0.7">
    <text x="16" y="18">ブランチに抱える</text>
    <text x="16" y="180">フラグでオフにして入れる</text>
  </g>
  <g fill="none" stroke="currentColor" stroke-width="1.5">
    <rect x="10" y="26" width="400" height="112" rx="6" fill="var(--vp-c-bg-soft, transparent)" />
    <rect x="10" y="188" width="400" height="100" rx="6" fill="var(--vp-c-bg-soft, transparent)" />
  </g>
  <g stroke="currentColor" stroke-width="1.5" fill="none">
    <path d="M40 60 H380" />
    <path d="M40 60 C120 60 140 110 380 110" stroke-dasharray="5 4" />
    <path d="M380 110 L380 66" marker-end="url(#d178-a)" />
    <path d="M40 222 H380" />
    <path d="M110 222 V240" /><path d="M180 222 V240" /><path d="M250 222 V240" /><path d="M320 222 V240" />
  </g>
  <defs>
    <marker id="d178-a" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M0 0 L10 5 L0 10 z" fill="currentColor" />
    </marker>
  </defs>
  <g fill="currentColor" font-size="11">
    <text x="40" y="52">統合先</text>
    <text x="40" y="126">作業ブランチ</text>
    <text x="255" y="88" opacity="0.75">離れていく</text>
    <text x="262" y="132">最後に大きなマージ</text>
    <text x="40" y="214">統合先</text>
    <text x="96" y="254" opacity="0.75">毎日入れる。機能はオフのまま</text>
  </g>
</svg>

未完成をフラグで隠しながら、小さくこまめに統合先へ入れ続ける進め方を**トランクベース開発**と呼びます。長生きするブランチを作らないための進め方です。

**出す日を選べる。** コードが入っている状態で待てるので、公開のタイミングを開発の進み方から切り離せます。

**少しずつ出せる。** 一度に全員へ出すと、問題があったとき全員が踏みます。1%、10%と少しずつ広げれば、踏む人を減らせます。

**すぐ止められる。** 問題が出たとき、切り戻しの手順を通さずにオフにできます。直して出し直すまでのあいだ、壊れた画面を見せ続けずに済みます。

## 4つの使い道

ここまでは新機能を出す話でしたが、同じ仕掛けは別の目的にも使われます。よく使われる整理では4つに分かれます。

| 種類 | 何のために | 寿命 |
|---|---|---|
| リリース | 未完成のコードを本番に置いておく | 短い。出したら消す |
| 実験 | A/B テストのように、利用者を分けて比べる | 一時的。決まったら消す |
| 運用 | 負荷が高いときに重い機能を止めるなど | 長い |
| 権限 | 契約プランや役割によって出し分ける | ずっと残る |

同じ `if (flag)` でも、寿命と切り替えの頻度が違います。数日で消すつもりのものと、ずっと残るものを同じ扱いにすると、あとで区別がつかなくなります。

A/B テストも、この表のとおりフラグの一種として実装されます。仕組みは同じ `if` 文で、どちらを見せるかを乱数で振り分けて、あとで結果の数字を比べます。

## 足すのは簡単で消すのは難しい

フラグを足すのは簡単です。分岐をひとつ書くだけです。難しいのは消すほうで、オンにして問題が出なければそのまま動いてしまうので、わざわざ触る理由がなく、放っておくと誰も消しません。

そして数が増えると、分岐は掛け算で増えます。

| フラグの数 | 組み合わせ |
|---|---|
| 1 | 2 通り |
| 3 | 8 通り |
| 10 | 1024 通り |

全部を試すことはできません。実際に確かめられるのは、そのうちのごく一部です。

## 2012年の証券会社

2012年、ある証券会社で実際に起きた事故です。9年間使われていなかった古い機能が、フラグと一緒にコードに残っていました。新しい機能を入れるとき、そのフラグの名前を**使い回した**のです。

配信作業で8台のうち1台に新しいコードが届かず、その1台では同じフラグが古い機能を起動しました。45分で約4.4億ドルの損失が出ました。監督官庁の処分では、使われなくなったコードの起動を防ぐ仕組みがなかったことが指摘されています。

配信の漏れとフラグの使い回しが重なった事故で、フラグだけが原因ではありません。ただ、9年放置されたフラグが残っていなければ、この形にはなりませんでした。

## 足す時点で消す条件を決める

だから、足す時点で決めておきます。何が起きたらこのフラグは要らなくなるのか、誰が消すのか、オンにしたあといつまで分岐を残すのか。

上の表の「寿命」の列が効くのはここです。ずっと残る権限のフラグと、出したら消すリリースのフラグを、同じ名前の付け方や同じ置き場所で管理すると、どれが消せるのか分からなくなります。

## まとめ

- フラグを挟むと、コードを出すことと機能を見せることが別々になる
- `if` 文だが値がコードの外にあるので、コードを変えずに、人によって切り替えられる
- 足すのは簡単で消すのは難しいので、入れる時点で消す条件を決める
