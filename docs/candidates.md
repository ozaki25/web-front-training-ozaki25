# コンテンツ候補

Day 19 まで公開済み。ADR（チームの設計判断記録）をベースに、レッスン候補を整理する。

## 未公開の下書き

| テーマ | 切り口 | ステータス |
|-------|--------|----------|
| 関数とコールバック | 関数の2つの書き方 → 関数は値 → コールバック → 高階関数 → コールバック地獄 | 作成済み（drafts/005） |
| Promise と async/await | シングルスレッド → 非同期が必要 → コールバック → Promise → async/await | 作成済み（drafts/006） |
| モジュールの歴史 | グローバルスコープ汚染 → CommonJS → ES Modules。なぜ 2 種類あるか | 作成済み（drafts/007） |
| Storybook 駆動開発 | 画面から作る辛さ → 部品から作る → Story が書きにくい部品は設計が悪いサイン | 作成済み（drafts/010） |

## ADR ベースの候補（1レッスン規模）

ADR の背景から「ここで聞かなければ知ることはなかった × 実務で出会う」を満たすもの。

### コード設計・構成

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| Server / Client 境界 | 012 | `'use client'` をどこに付けるか。サーバー専用コードがブラウザに漏れると何が起きるか |
| ディレクトリ構成 | 009, 010 | feature 単位でコードを切る発想。なぜ components/ に全部入れないのか |
| import 制限 | 011, 014 | なぜ好きなファイルを好きに import してはいけないのか。依存方向と barrel file |
| コンポーネント実装方針 | 018 | variant / size / disabled をどう表現するか。cva / tailwind-variants の発想 |
| 命名規則 | 013 | PascalCase / kebab-case / camelCase、なぜ揃える必要があるか |

### データ・通信

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| REST API 連携 | 030 | フロントから API を叩くとはどういうことか。リクエスト/レスポンス/ステータスコード |
| gRPC とは | 028, 029 | REST じゃない通信方式。Proto ファイルと型生成。なぜ選ばれるか |
| エラーハンドリング | 031, 044, 045 | データ取得が失敗したとき何をするか。エラーコード、エラー画面 |
| フォームとバリデーション | 033, 034, 035, 036 | 入力値の検証はどこでやるか（クライアント / サーバー / 両方）。Server Actions との統合 |
| 状態管理 | 026, 027 | グローバル状態とは。URL に状態を持つという発想 |
| キャッシュ設計 | 023 | ブラウザ / CDN / サーバーのキャッシュ階層。なぜ「古いデータ」が表示されるか |

### セキュリティ・認証

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| 認証 | 037, 039 | ログインの裏側。Cookie、トークン、なぜログイン状態が保たれるか |
| 認可（RBAC） | 038 | 「この人はこの画面を見ていいか」の仕組み。ロールベースアクセス制御 |
| XSS / CSRF | 041 | 攻撃が成立する仕組み。なぜ対策が必要か |
| セキュリティヘッダー | 040 | ブラウザに「これをするな」と伝えるヘッダー |
| 環境変数 | 016 | `.env` と `NEXT_PUBLIC_` の意味。秘密情報がブラウザに漏れる仕組み |

### テスト（複数レッスンに分割）

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| なぜテストを書くか | 049 | テストがないと何が起きるか。コンポーネントテストの考え方 |
| テストの種類 | 048, 049, 055 | 単体 / 結合 / E2E の違い。何をどこまでテストするか |
| VRT（ビジュアルリグレッション） | 054 | CSS を直したら別の画面が壊れた問題。見た目の差分を自動検知 |
| a11y テスト | 050 | アクセシビリティの自動チェック。Storybook アドオンや axe |
| テストカバレッジ | 056 | 100% を目指すべきか。カバレッジの数字が意味すること |
| モック | 051, 052 | 外部 API が存在しない状態で開発する方法。REST / gRPC のモック |

### 技術選定（複数レッスンに分割）

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| ライブラリの選び方 | 006 | ライセンス、保守状況、人気度、バンドルサイズ。何を見て判断するか |
| ライブラリをラップするか | 007 | 直接使う vs 薄いラッパーを作る。差し替えやすさと開発速度のトレードオフ |
| UI ライブラリ選定 | 017 | ヘッドレス（React Aria / Radix）vs フル UI（shadcn/ui / MUI）。何が違うか |
| 日付ライブラリ | 008 | dayjs / date-fns / Temporal。なぜ標準の Date では足りないか |

### インフラ・運用

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| モノレポ | 004 | 1つのリポジトリに複数のパッケージ。なぜ分けるのか |
| パッケージマネージャ | 005 | npm / pnpm / yarn の選定。Day 17 の延長 |
| CI/CD | 059 | push したらテスト → ビルド → デプロイが自動で走る仕組み |
| ブランチ戦略 | 058 | main / develop / feature。なぜブランチを分けるか |
| ロギング | 042, 043 | サーバーとクライアントのログ。なぜ記録するか |
| 監視 | 046, 047 | 本番で何が起きているか見る方法。パフォーマンス監視 |

### デザイン・見た目

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| デザイントークン | 019 | 色・サイズ・余白を変数で管理する。なぜハードコードしないか |
| Baseline 採用基準 | 020 | ブラウザ対応状況をどう判断するか。使っていい CSS/JS の基準 |
| 画像最適化 | 025 | next/image の裏側。なぜ画像が重いか、どう軽くするか |
| ローディング戦略 | 024 | Skeleton / Spinner / loading.tsx。ユーザーを待たせない見せ方 |

### AI・開発プロセス

| テーマ | ADR | 切り口 |
|-------|-----|--------|
| AI コーディング支援 | 003 | CLAUDE.md やルールファイルの意味。AI に何を伝え、何を伝えないか |
| ADR とは | 001 | なぜ設計判断を記録するのか。新人が過去の判断を追える仕組み |
| scaffolding | 061 | ファイルの自動生成。なぜ手で作らないか |
| ドキュメント | 060 | 何をどこに書くか。コードの中 vs 外のドキュメント |
| リンター・フォーマッター | 057 | ESLint / Prettier。なぜコードの書き方を機械で強制するか |
