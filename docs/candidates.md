# コンテンツ候補

Day 19 まで公開済み。

## 未公開の下書き

公開待ちの下書き。内容・順番は未確定。

| テーマ | 切り口 | ステータス |
|-------|--------|----------|
| 関数とコールバック | 関数の2つの書き方 → 関数は値 → コールバック → 高階関数 → コールバック地獄 | 作成済み（drafts/005） |
| Promise と async/await | シングルスレッド → 非同期が必要 → コールバック → Promise → async/await | 作成済み（drafts/006） |
| モジュールの歴史 | グローバルスコープ汚染 → CommonJS → ES Modules。なぜ 2 種類あるか | 作成済み（drafts/007） |
| Storybook 駆動開発 | 画面から作る辛さ → 部品から作る → Story が書きにくい部品は設計が悪いサイン | 作成済み（drafts/010） |

## 検討中

| テーマ | 切り口 | ステータス |
|-------|--------|----------|
| ビルド | ブラウザが理解できるのは HTML/CSS/JS だけ。import 解決・トランスパイル・バンドル・最適化 | 検討中 |
| Server / Client Components | コードがどこで動くか。`'use client'`、なぜ Server で useState が使えないか | 有力 |

## 大きめのテーマ（複数レッスンに分割が必要）

1 レッスンには収まらない。分割設計してから着手する。

| テーマ | 切り口 |
|-------|--------|
| テスト | なぜ書くか / 単体・結合・E2E / 何をどこまで / テストしやすい設計 |
| 技術選定 | 選ぶ基準（実績・保守・コミュニティ）/ トレードオフ / なぜ React・Next.js なのか |
