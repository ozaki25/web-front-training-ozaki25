# コンテンツ候補

Day 17 まで公開済み。ここから Next.js までの道筋を整理する。

## 未公開の下書き

公開待ちの下書き。内容・順番は未確定。

| テーマ | 切り口 | ステータス |
|-------|--------|----------|
| 関数とコールバック | 関数の2つの書き方 → 関数は値 → コールバック → 高階関数 → コールバック地獄 | 作成済み（drafts/005） |
| Promise と async/await | シングルスレッド → 非同期が必要 → コールバック → Promise → async/await | 作成済み（drafts/006） |
| モジュールの歴史 | グローバルスコープ汚染 → CommonJS → ES Modules。なぜ 2 種類あるか | 作成済み（drafts/007） |
| 宣言的 UI | 命令的な DOM 操作の辛さ → 宣言的 UI という発想の転換 → 仮想 DOM | 作成済み（drafts/008） |

## 検討中

| テーマ | 切り口 | ステータス |
|-------|--------|----------|
| ビルド | ブラウザが理解できるのは HTML/CSS/JS だけ。import 解決・トランスパイル・バンドル・最適化。15分に収まるか要検討 | 検討中 |
| Server / Client Components | コードがどこで動くか。`'use client'`、なぜ Server で useState が使えないか。実行環境の話と接続 | 有力 |
