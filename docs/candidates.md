# 来週以降のコンテンツ候補

## 確定・作成中

- **npm**（drafts/001） — 3 つの意味、パッケージ、package.json、node_modules。作成済み、レビュー中

## 有力候補

- **Promise と非同期処理** — コールバック → Promise → async/await の変遷。イベントループにも触れる。難しいが必須
- **関数の変遷と使い分け** — function → アロー関数。`this` の束縛問題、使い分け
- **動的型付け** — JS は型がなくても動く。なぜそうなっているか、何が起きるか

## 検討中

- **なぜ React が生まれたのか** — DOM 操作の大変さ → 宣言的 UI。jQuery → React の変遷
- **なぜビルドが必要なのか** — HTML/CSS はそのまま動く。なぜ Next.js は変換が必要か
- **なぜ型が必要になったのか（TypeScript）** — 動的型の話の延長で

## 保留

- **イベントループ** — 単独では「知ってどうする」が弱い。Promise の中で触れる方が自然
- **`for` → `.map()` / `.filter()`** — React の JSX 内で多用するので伏線になるが、深みが出にくい
- **`require` → `import`** — モジュールの変遷。Day 13 の ECMAScript の話と繋がるが単独か微妙
